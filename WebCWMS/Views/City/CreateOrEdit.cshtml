@*@model WebCWMS.Models.City
@{
	ViewData["Title"] = Model != null && Model.Id == 0 ? "Create City" : "Edit City";
}
<form method="post" action="CreateOrEdit" enctype="multipart/form-data">
	<div class="card">
		<div class="card-header bg-info">
			@ViewData["Title"]
		</div>
		<div class="card-body">
			<input type="hidden" asp-for="Id" />

			<div class="form-floating mb-2">
				<input asp-for="CityName" class="form-control" />
				<label asp-for="CityName"></label>
				<span asp-validation-for="CityName" class="text-danger"></span>
			</div>

			<div class="form-group">
				<label class="control-label">Avenue</label>
				<select asp-for="AvenueId" asp-items="ViewBag.Avenues" class="form-control"></select>
				<span class="text-danger">@Html.ValidationMessageFor(model => model.AvenueId)</span>
			</div>

			

		</div>
		@if (Model != null && Model.Id == 0)
		{
			<div class="col-lg-12 d-grid">
				<button type="submit" class="btn btn-primary">Submit</button>
			</div>

		}
		else
		{
			<div class="col-lg-12 d-grid">
				<button type="submit" class="btn btn-success">Update</button>
			</div>

		}

	</div>
</form>
@section Scripts{
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}*@